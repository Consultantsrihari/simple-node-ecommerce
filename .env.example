# MongoDB Connection String (replace with your actual connection string)
# For local MongoDB: MONGODB_URI=mongodb://localhost:27017/shop
# For MongoDB Atlas: MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-url>/shop?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/shop

# Secret used for session signing
SESSION_SECRET=mySuperSecretSessionKeyLongerIsBetter

# Port the application will run on
PORT=3000
