<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css"> <!-- Assuming you might create a forms.css -->
    <link rel="stylesheet" href="/css/auth.css"> <!-- Assuming you might create an auth.css -->
</head>

<body>
   <main>
        <% if (errorMessage) { %>
            <div class="error-message"><%= errorMessage %></div>
        <% } %>
        <form class="login-form" action="/auth/login" method="POST">
             <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="form-control">
                <label for="email">E-Mail</label>
                <input type="email" name="email" id="email" value=""> <!-- Add value="<%= oldInput.email %>" if implementing validation feedback -->
            </div>
            <div class="form-control">
                <label for="password">Password</label>
                <input type="password" name="password" id="password">
            </div>
            <button class="btn" type="submit">Login</button>
        </form>
         <div class="centered">
             <a href="/auth/reset">Forgot Password?</a> <!-- Link added, but functionality not implemented -->
         </div>
    </main>
<%- include('../includes/end.ejs') %>
